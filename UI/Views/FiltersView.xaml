<UserControl x:Class="UI.Views.FiltersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:utils="clr-namespace:UI.Utilities">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Поисковая панель -->
            <RowDefinition Height="Auto"/> <!-- Фильтры -->
        </Grid.RowDefinitions>
        
        <Border Style="{StaticResource Bord}">
            <TextBox utils:TextBoxHelper.Placeholder="Search..."
                     Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"/>
        </Border>
        
        <Grid Grid.Row="1">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            
        </Grid>
    </Grid>
</UserControl>